{
  "mute_max_seconds": {
    "description": "禁言时长上限（秒）",
    "type": "int",
    "default": 86400,
    "hint": "LLM 调用 mute_user 工具时，禁言时长不会超过此值。默认 86400（1天）"
  },
  "silence_scope_default": {
    "description": "屏蔽默认作用域",
    "type": "string",
    "default": "session",
    "hint": "LLM 调用 silence_user 工具时的默认作用域：session（仅当前会话）或 global（全局）"
  },
  "avatar_size": {
    "description": "头像尺寸",
    "type": "int",
    "default": 640,
    "hint": "获取头像的尺寸（像素），常见值：100/640/1000"
  },
  "avatar_download_timeout": {
    "description": "头像下载超时时间（秒）",
    "type": "int",
    "default": 10,
    "hint": "下载头像的超时时间"
  },
  "avatar_vision_provider_id": {
    "description": "头像识别模型提供商",
    "type": "string",
    "default": "",
    "_special": "select_provider",
    "hint": "用于 get_user_avatar 工具内二次视觉识别。留空时使用当前会话模型。建议选择支持图片输入的 Provider"
  },
  "image_context_vision_provider_id": {
    "description": "图片上下文识别模型提供商",
    "type": "string",
    "default": "",
    "_special": "select_provider",
    "hint": "用于 get_recent_image_context 工具内二次视觉识别。留空时回退到头像识别模型或当前会话模型"
  },
  "image_context_history_rounds": {
    "description": "图片检索历史轮数",
    "type": "int",
    "default": 3,
    "hint": "get_recent_image_context 查询群历史消息的轮数（每轮按 lookback_count 查询）"
  }
}
